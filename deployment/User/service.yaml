apiVersion: v1
kind: Service
metadata:
  name:  user-service
  labels:
    app:  user
spec:
  selector:
    app:  user
  type:  NodePort
  ports:
    - port:  80
      targetPort:  8000
      nodePort: 31000
      protocol: TCP
